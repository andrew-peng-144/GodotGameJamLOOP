extends Node

@export var player_body: CharacterBody2D


#var coins = 0
#@export var coins_label: Label # if GameManager is autoloaded, it wont take in exported vars?
#
#func add_point():
	#coins += 1
	##print(coins_label)
	#coins_label.text = "" + str(coins) + " coins"
